export type Role = 'citizen' | 'operator' | 'auditor' | 'official';
export interface User {id: string; name: string; email: string; role: Role; department?: string; subject?: string; designation?: string; unit?: string; unit_name?: string}
export interface StageReview {requested_at: string; mode: 'manual'|'auto'; decision?: 'SANCTIONED'|'REJECTED'; remarks?: string; officer_name?: string; designation?: string; decided_at?: string}
export interface Stage {id:string; name:string; system:string; connector:string; state:string; external_id?:string; started_at?:string; completed_at?:string; operation_id:string; attempts:any[]; evidence?:any; policy?:any; error?:string; review?:StageReview|null}
export interface Application {id:string; transaction_id:string; person_reference:string; owner_name:string; service_code:string; service_name:string; option_code:string; option_label:string; department:string; unit:string; district:string; status:string; created_at:string; updated_at:string; version:number; stages:Stage[]; events:any[]; audit:any[]; consents:any[]; mappings:any[]; canonical:any; scenario?:string; review_mode?:string; next_retry_at?:string; amount:number; is_demo:boolean}
export interface Scheme {code:string; name:string; unit:string; department:string; system:string; amount:number; description:string; eligibility:string; option_label:string; options:{code:string; label:string}[]; districts:string[]; stages:{id:string; name:string; system:string}[]; icon:string}
export const homePath = (u: {role: string} | null) => !u ? '/login' : u.role === 'citizen' ? '/citizen' : u.role === 'official' ? '/operations/reviews' : '/operations';
export const rupees = (n: number) => `₹${n.toLocaleString('en-IN')}`;
